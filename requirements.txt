# Core runtime dependencies (CLI). Optional surfaces:
#   TUI: pip install 'wifi-jammer[tui]'   (textual)
#   GUI: pip install 'wifi-jammer[gui]'   (PyQt6)
scapy>=2.6.1
psutil
colorama
rich
click
pyyaml
cryptography>=45.0.6
